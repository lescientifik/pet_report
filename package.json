{
  "name": "pet-report",
  "version": "1.0.0",
  "type": "module",
  "description": "Générateur de comptes rendus TEP-FDG standardisés",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && touch dist/.nojekyll && gh-pages -d dist -t true"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "gh-pages": "^6.1.0",
    "vite": "^5.0.0"
  }
}
